name: deploy-dev

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          role-session-name: gh-actions-${{ github.run_id }}
          aws-region: eu-central-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.9"

      - name: Terraform init
        run: terraform init
        working-directory: infra

      - name: Terraform plan
        run: terraform plan -out=plan.tfplan
        working-directory: infra

      # ─── Apply is manual for first deploy ───────────────────────────
      # Uncomment below after first manual terraform apply succeeds
      # and the OIDC role exists:
      #
      # - name: Terraform apply
      #   run: terraform apply -auto-approve plan.tfplan
      #   working-directory: infra
      #
      # - name: Run migrations against RDS
      #   run: |
      #     DB_PASSWORD=$(aws secretsmanager get-secret-value \
      #       --secret-id /relowa/dev/db/master-password \
      #       --query SecretString --output text)
      #     RDS_HOST=$(terraform -chdir=infra output -raw rds_endpoint)
      #     DATABASE_URL="postgres://relowa:${DB_PASSWORD}@${RDS_HOST}:5432/relowa" \
      #       pnpm db:migrate
      # - name: RLS isolation test (live RDS)
      #   run: |
      #     DB_PASSWORD=$(aws secretsmanager get-secret-value \
      #       --secret-id /relowa/dev/db/master-password \
      #       --query SecretString --output text)
      #     RDS_HOST=$(terraform -chdir=infra output -raw rds_endpoint)
      #     PSQL_HOST=$RDS_HOST PSQL_PASSWORD=$DB_PASSWORD ./tests/rls-isolation.sh
