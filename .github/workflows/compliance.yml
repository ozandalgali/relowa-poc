name: compliance

on:
  schedule:
    - cron: "0 6 * * 0" # 06:00 UTC Sunday
  workflow_dispatch:

jobs:
  compliance:
    # Inert P2 — activate when KVKK assertions graduate
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Compliance checks — not yet active"
