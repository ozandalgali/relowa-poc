name: integration

on:
  pull_request:
    types: [labeled, synchronize]
  push:
    branches: [main]

jobs:
  integration:
    # Inert until apps/api exists — guarded by [full] label or main push
    if: false
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: relowa
          POSTGRES_PASSWORD: dev_password_change_me
          POSTGRES_DB: relowa
        ports:
          - 5433:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm db:reset
      - run: pnpm test:integration
      - run: pnpm test:sfn
      - run: pnpm test:contract
