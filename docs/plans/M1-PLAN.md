# M1 Plan — Auth, Data & RLS Substrate

> **Agent:** migration-author, rls-test-runner, ci-cd-engineer, doc-keeper
> **Lead:** lead-orchestrator (approved 2026-05-15)
> **Target:** Weeks 4-6 per PRD-0003

## Status

| Step | Status |
|------|--------|
| 1. Schema additions (14 tables, 6 enums) | 🔨 Done |
| 2. Drizzle generate + raw SQL RLS side-car | ⬜ Done |
| 3. relowa_admin DB role (raw SQL) | ⬜ Done |
| 4. Register raw SQL in migrate.ts | ⬜ Done |
| 5. Extend RLS isolation tests | ⬜ Done |
| 6. Terraform: enable RDS backups | ⬜ Done |
| 7. db:reset + verify locally | ⬜ Done |
| 8. CHANGELOG + docs | ⬜ Done |

## Dependency graph

```
[migration-author] → [Drizzle generate]
                    ↓
              [raw SQL side-car] + [relowa_admin role]
                    ↓
              [migrate.ts RAW_SQL_FILES]
                    ↓
              [rls-test-runner] ←→ [ci-cd-engineer: RDS backups]
                    ↓
              [db:reset + rls-isolation.sh]
                    ↓
              [doc-keeper]
```

## Step 1 — Schema additions

### New enums (6)
- `staff_role`: super_admin, account_manager, support_agent, compliance_officer, financial_analyst
- `risk_level`: low, medium, high, critical  
- `carrier_ad_status`: open, closing, awarded, cancelled, expired
- `carrier_bid_status`: submitted, withdrawn, rejected, accepted
- `shipment_status`: pending, in_transit, delivered, disputed, completed
- `escrow_status`: pending, funds_locked, in_transit, delivered, released, refunded, disputed, failed

### New tables (14)
- Staff RBAC: `internal_staff`, `staff_org_assignments`, `staff_permissions`, `staff_role_permissions`, `admin_audit_log`
- Carrier: `carrier_ads`, `carrier_bids`, `shipments`, `shipment_events`
- Escrow: `escrow_orders`, `escrow_transactions`, `provider_webhooks`
- Outbox: `outbox`
- Anchor: `anchor_log`

## Manual steps (1)

| # | When | Action |
|---|------|--------|
| 1 | After step 6 (ci-cd-engineer) | `terraform apply` in `infra/` to enable RDS backups |


## 🔴 Manual steps (human action required)

| # | When | Action | Why |
|---|------|--------|-----|
| 1 | After M1 schema merge | Run `pnpm db:reset` to apply all migrations locally | Verify schema deploys cleanly |
| 2 | After bastion deploy | Download SSH key from Secrets Manager: `aws secretsmanager get-secret-value --secret-id /relowa/dev/bastion/ssh-private-key --query SecretString --output text --profile relowa > ~/.ssh/relowa-bastion.pem && chmod 600 ~/.ssh/relowa-bastion.pem` | Bastion SSH key auto-generated by Terraform |
| 3 | After RDS backup config | Run `terraform apply` in `infra/` to enable backup retention | RDS backup_retention_period changes require apply |
| 4 | After live RDS migration | Run `DATABASE_URL="postgres://relowa:<password>@127.0.0.1:5433/relowa?sslmode=require" pnpm db:migrate && pnpm db:seed` | Schema must be deployed to RDS before API uses it |
